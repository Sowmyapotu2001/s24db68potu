extends layout

block content
  h1= title
  - let items = results
  div(class="container-fluid")
    div(class="row m-1")
      each item, index in results 
        -let destination = `dispatch(\"/helmets/update?id=${item._id}\")` 
        div(class="col-sm-2",onclick=`buy(${index})`)
          ul
            li #{item.helmet_style} 
            li #{item.size}
            li #{item.price}
        button.btn( class="btn-primary ml-1", onclick= destination) Update

  script.
    function dispatch(path){
      window.location.href=path
    }



//- extends layout

//- block content
//-   h1= title
//-   - let items = results
//-   //- - let results = [{name: "Maldives", location: "South Asia", rating: 4.9},{name: "Bora Bora", location: "France", rating: 4.8},{name: "Maui", location: "USA", rating: 4.7}]
//-   div(class="container-fluid")
//-     div(class="row")
//-       each item,index in results
//-         div(class="col-sm-2")
//-           h4 item #{index + 1}
//-           p #{item.style} - #{item.size} - #{item.price}
//-           div(onclick=`buy(${index})`, class="btn btn-primary") Buy
  
//-   script. 
//-     function buy(index) {
//-       alert(`You purchased item ${index + 1}`);
//-     }
